<%= form_with(model: libro, local: true) do |form| %>
  <% if libro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(libro.errors.count, "error") %> prohibited this libro from being saved:</h2>

      <ul>
      <% libro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="text-center">
   <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

 <div class="form-group">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

   <div class="form-group">
    <%= form.label :estado%>
    <%= form.select :estado, options_for_select(['en estante', 'prestado'])%>

  </div>


 <div class="form-group">
    <%= form.label :fecha_prestamo %>
    <%= form.date_select :fecha_prestamo %>
  </div>

 <div class="form-group">
    <%= form.label :fecha_devoulto %>
    <%= form.date_select :fecha_devoulto %>
  </div>

  <div class="actions">
    <button type="button" class="btn text-white bg-dark "><%= form.submit %></button>
  </div>


<% end %>
  </div>
